# Codex

Read `AGENTS.md` first, then `AGENT_CONFIG.md`.

Your role definition is `.agents/roles/codex.md`.

Do not duplicate rules here. Portable behavior lives in `AGENTS.md`, `WORKFLOW.md`, and `.agents/`; repo-specific behavior lives in `AGENT_CONFIG.md`.
