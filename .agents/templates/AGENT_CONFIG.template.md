# Agent config

The single source of truth for repo-specific agent behavior. Generated by the `bootstrap` skill; re-run `bootstrap` to refresh it.

Portable behavior lives in `AGENTS.md`, `WORKFLOW.md`, `.agents/roles/`, and `.agents/skills/`. How humans run this repo lives in `README.md`. Neither is duplicated here.

## Project identity

<PLACEHOLDER: one paragraph — what this repo is, what it is not, and anything an agent would otherwise get wrong.>

## Repo overview document

<PLACEHOLDER: path to the document describing this codebase's purpose, architecture, and domain background — e.g. `ARCHITECTURE.md`, `docs/design.md`, or `none`.>

Do not assume this file is called `ARCHITECTURE.md`. If it is `none`, offer to generate one with the `summarize` skill; do not create it unasked.

## Environment

Interpreter / environment command:

```bash
<PLACEHOLDER>
```

## Tests

Default targeted test command:

```bash
<PLACEHOLDER>
```

<PLACEHOLDER: where tests live, what counts as "targeted" here, anything untested that agents should not assume is covered.>

## Do not modify

Agents must not modify these unless explicitly asked:

```text
<PLACEHOLDER>
```

Temporary files go in `<PLACEHOLDER: ./tmp/>`.

## Git policy

- Staging allowed: `<PLACEHOLDER: yes/no>`
- Committing allowed: `<PLACEHOLDER: yes/no>`
- Pushing allowed: `<PLACEHOLDER: yes/no>`
- Branch policy: `<PLACEHOLDER>`

When staging is allowed, use exact scoped commands (`git add -- <files>`), never `git add .`.

## Expensive or dangerous operations

Never run these unless the user explicitly asks:

```text
<PLACEHOLDER: training runs, cluster submissions, paid API calls, data downloads, destructive scripts>
```

## Enabled agents

| Agent | Enabled | Stages | Invocation |
|---|---|---|---|
| Claude | yes | all | — |
| Codex | <PLACEHOLDER: yes> | <PLACEHOLDER> | `<PLACEHOLDER>` |
| Grok | <PLACEHOLDER: no> | <PLACEHOLDER> | `<PLACEHOLDER>` |
| Antigravity | <PLACEHOLDER: no> | <PLACEHOLDER> | `<PLACEHOLDER>` |

## Issue tracker

<PLACEHOLDER: tracker, workspace/team, project name, project ID, and any scoping rule for creating or updating issues. Write "none" if there is no tracker.>

This is what enables the `intake` skill and the issue-resolution workflow.

## Workflows

Default: **idea to code** (`.agents/workflows/idea-to-code.md`).

<PLACEHOLDER: any repo-specific constraint on which workflows may run here — for example, whether the autonomous issue-resolution workflow is permitted at all.>

## Domain-specific review risks

Reviewers should always check for:

```text
<PLACEHOLDER: the correctness risks that matter in this repo's domain>
```

## Repo-specific documents

```text
<PLACEHOLDER: e.g. ARCHITECTURE.md, README.md, and what each covers>
```
